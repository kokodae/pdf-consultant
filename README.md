# PDF Consultant with GigaChat

Веб-приложение для работы с PDF-документами на базе RAG-архитектуры. Позволяет загрузить технический документ или учебное пособие, задать вопрос и получить ответ с точными ссылками на исходный текст.

## Функционал

- Загрузка PDF-файлов объёмом 100-200 страниц
- Автоматическая индексация текста с сохранением нумерации страниц
- Диалоговый интерфейс с поддержкой истории сообщений
- Генерация ответов на основе контекста документа
- Отображение цитат с указанием номера страницы для каждого утверждения
- Автоматическое определение названия документа при загрузке
- Сброс сессии с полной очисткой векторного хранилища
- 
## Демонстрация

![Демонстрация]([./demo.mp4](https://github.com/kokodae/pdf-consultant/blob/main/demo.mp4))

## Стек

- LangGraph — оркестрация пайплайна: состояние, ветвления, обработка ошибок
- ChromaDB — векторное хранилище, работающее в одном процессе с приложением
- EmbeddingsGigaR — эмбеддинги от GigaChat, оптимизированные под русскоязычные технические тексты
- GigaChat / GigaChat-2-Max — генерация ответов и извлечение названия документа
- Streamlit — фронтенд: чат, загрузка файлов, кастомные стили
- Docker — контейнеризация, воспроизводимая сборка, изоляция зависимостей
- Pydantic + json.loads — парсинг структурированных ответов с фоллбэком на неструктурированный текст

## Структура проекта
pdf-consultant/
├── backend/
│   ├── config.py               # Конфигурация и env-переменные
│   ├── gigachat/               # Клиенты и авторизация GigaChat
│   │   ├── auth.py            # OAuth2-токен
│   │   ├── clients.py         # REST-клиенты
│   │   └── factory.py         # Выбор клиента
│   ├── rag/                    # RAG-компоненты
│   │   ├── embeddings.py      # EmbeddingsGigaR
│   │   ├── llm_adapter.py     # Адаптер под LangChain
│   │   ├── graph.py           # LangGraph pipeline
│   │   └── vector_store.py    # ChromaDB
│   └── utils/
│       └── pdf_processor.py   # Парсинг и чанкинг PDF
├── frontend/
│   └── app.py                  # Streamlit UI
├── .env                        # Переменные окружения
├── .env.example                # Шаблон конфигурации
├── requirements.txt            # Зависимости
├── Dockerfile                  # Образ приложения
├── docker-compose.yml          # Оркестрация
└── demo.mp4                    # Видеодемонстрация

## Быстрый запуск

```bash
# Настроить окружение
cp .env.example .env
# Указать GIGACHAT_API_KEY в формате Base64(client_id:client_secret)

# Запустить
docker compose up --build -d

# Открыть в браузере
# http://localhost:8501
